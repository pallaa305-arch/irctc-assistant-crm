version: '3.8'

services:
  irctc-assistant:
    build: .
    container_name: irctc-bot-24x7
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - TELEGRAM_ENABLED=true
      - DEMO_MODE=false
      - BROWSER_HEADLESS=true
